apiVersion: v2
name: fastapi
description: Chart générique pour un microservice FastAPI de l'application SkyBlue.
# Le même chart est instancié trois fois (users, orders, gateway) : seules
# changent l'image, les variables d'environnement et l'exposition du Service.
type: application
version: 0.2.0
appVersion: "1.0.0"
